# This starts your worker process (e.g., job queue processing)
worker: /go/bin/app

# Optionally, ensure the Datadog Agent is started if it doesn't start automatically
datadog-agent: service datadog-agent start